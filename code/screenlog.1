run analyzeExperimentData.m

T = 

    simParams: {21000x1 cell}

{Error using .*
Matrix dimensions must agree.

Error in analyzeExperimentData (line 144)
            allLinkTypes(ii,jj,:,kk,2) = sum((typesAll{ii}(:,jj)==kk).*(extctBinArray(LL(:,1),jj,:,ii).*extctBinArray(LL(:,2),jj,:,ii)));

Error in run (line 96)
evalin('caller', [script ';']);
} 
>> size(sum sypes         e(typesAll{ii}(:,jj)==kk)

ans =

   250     1

>> l L

L =

   250

>> size(extctBinArray(LL(:,1),jj,:,ii))

ans =

   250     1    16

>> size(extctBinArray(LL(:,1),jj,:,ii))L                                   size(typesAll{ii}(:,jj)==kk)run analyzeExperimentData.m size(repmat(fParAll.*nFree)run analyzeExperimentData.m

T = 

    simParams: {21000x1 cell}

{Array dimensions must match for binary array op.

Error in analyzeExperimentData (line 216)
netExtctPerPara = permute(squeeze(sum(extctDiff))./[nan(1,1,nWeb); -diff(nPara)],[3 1 2]);

Error in run (line 96)
evalin('caller', [script ';']);
} 
>> 